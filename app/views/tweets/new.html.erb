<div class="container">
  <div class="row justify-content-center">
    <div class="new-tweet">ツイート</div>
  </div>
  <div class="tweet-edit mt-5">
    <div class="row justify-content-center">
      <div class="col-12">
        <%= link_to "", tweets_path, class: "browser-back far fa-arrow-alt-circle-left", style: "text-decoration: none; color: #444444;" %>
      </div>
      <div class="col-12 col-lg-9">
        <%= render 'layouts/errors', obj: @tweet %>
        <%= form_with model: @tweet, local:true do |f| %>
          <div class="form-group mt-5">
            <h4 class="mt-3">ツイート内容</h4>
            <%= f.text_area :body, class: 'form-control', style: "height: 150px;" %>
          </div>
          <div class="form-group mt-5">
            <h4 class="mt-3">タグ</h4>
            <div>半角スペースを入力することで複数のタグをつけることができます。</div>
            <div>例:アプデ LMG 加藤純一</div>
            <%= f.text_field :tag_name, class: "form-control" %>
          </div>
          <div class="form-group mt-5">
            <h4>ツイート画像</h4>
            <%= f.attachment_field :image, class: "form-control-file" %>
          </div>
          <div class="row justify-content-center">
            <div class="form-group col-5 mt-5">
              <%= f.submit 'ツイートする', class: "btn btn-block btn-primary" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>